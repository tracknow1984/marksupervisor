const express=require('express');
const router=express.Router();
const {page}=require('../layout');
const {assets,assetTypes}=require('../store');

router.get('/api/assets',(req,res)=>res.json(assets));
router.get('/api/asset-summary',(req,res)=>res.json({total:assets.length,active:assets.filter(a=>a.status==='In Service').length,serviceDue:assets.filter(a=>a.serviceDue).length,openDefects:assets.reduce((n,a)=>n+(Number(a.openDefects)||0),0)}));
router.post('/api/assets',(req,res)=>{const {type,rego,make,model,location,status,reading}=req.body||{};if(!type||!rego||!make||!model)return res.status(400).json({error:'type, registration, make and model are required'});if(assets.some(a=>a.rego.toLowerCase()===String(rego).toLowerCase()))return res.status(409).json({error:'registration already exists'});const id=String(rego).trim().toUpperCase();const asset={id,name:`${make} ${model}`,make,model,type,rego:id,location:location||'',status:status||'In Service',serviceDue:false,openDefects:0,reading:Number(reading)||0};assets.push(asset);res.status(201).json(asset)});
router.put('/api/assets/:id',(req,res)=>{const asset=assets.find(a=>a.id===req.params.id);if(!asset)return res.status(404).json({error:'Asset not found'});const {type,rego,make,model,location,status,reading,serviceDue}=req.body||{};if(!type||!rego||!make||!model)return res.status(400).json({error:'type, registration, make and model are required'});const duplicate=assets.find(a=>a.id!==asset.id&&a.rego.toLowerCase()===String(rego).toLowerCase());if(duplicate)return res.status(409).json({error:'registration already exists'});asset.type=type;asset.rego=String(rego).trim().toUpperCase();asset.make=make;asset.model=model;asset.name=`${make} ${model}`;asset.location=location||'';asset.status=status||'In Service';asset.reading=Number(reading)||0;asset.serviceDue=Boolean(serviceDue);res.json(asset)});

router.get('/assets',(req,res)=>res.send(page('assets','Assets',`<section class="title"><div><h1>Assets</h1><p>Manage vehicles, plant, machinery and equipment across your operations.</p></div><button class="primary" id="addAssetBtn">＋ Add Asset</button></section><section class="cards"><div class="card"><label>Total Assets</label><strong id="total"></strong></div><div class="card"><label>Active</label><strong id="active"></strong></div><div class="card"><label>Due for Service</label><strong id="serviceDue"></strong></div><div class="card"><label>Open Defects</label><strong id="openDefects"></strong></div></section><div class="toolbar"><input id="search" placeholder="Search assets..."><select id="typeFilter"><option value="">All Asset Types</option>${assetTypes.map(x=>`<option>${x}</option>`).join('')}</select></div><section class="panel"><div class="tablewrap"><table><thead><tr><th>Asset</th><th>Type</th><th>Registration</th><th>Location</th><th>Service</th><th>Defects</th><th>Status</th><th>Actions</th></tr></thead><tbody id="rows"></tbody></table></div></section><div class="modal" id="assetModal"><div class="box"><div class="modalhead"><h2 id="modalTitle">Add Asset</h2><button class="close" id="closeModal">×</button></div><div class="formbody"><div class="grid"><div class="field"><label>Asset Type *</label><select id="fType">${assetTypes.map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field"><label>Registration *</label><input id="fRego"></div><div class="field"><label>Make *</label><input id="fMake"></div><div class="field"><label>Model *</label><input id="fModel"></div><div class="field"><label>Odometer / Hours</label><input id="fReading" type="number"></div><div class="field"><label>Status</label><select id="fStatus"><option>In Service</option><option>Maintenance</option><option>Decommissioned</option><option>Retired</option></select></div><div class="field full"><label>Location</label><input id="fLocation"></div><label class="toggle full"><input type="checkbox" id="fServiceDue"> Due for service</label></div></div><div class="actions"><button class="secondary" id="cancelModal">Cancel</button><button class="primary" id="saveAsset">Save Asset</button></div></div></div><script>(()=>{const $=id=>document.getElementById(id);let list=[],editingId=null;async function load(){list=await fetch('/api/assets').then(r=>r.json());const s=await fetch('/api/asset-summary').then(r=>r.json());$('total').textContent=s.total;$('active').textContent=s.active;$('serviceDue').textContent=s.serviceDue;$('openDefects').textContent=s.openDefects;render()}function render(){const q=$('search').value.toLowerCase();const t=$('typeFilter').value;$('rows').innerHTML=list.filter(a=>(!t||a.type===t)&&(!q||[a.name,a.type,a.rego,a.location].join(' ').toLowerCase().includes(q))).map(a=>'<tr><td><b>'+a.name+'</b><div class="sub">'+a.make+' '+a.model+'</div></td><td>'+a.type+'</td><td>'+a.rego+'</td><td>'+(a.location||'—')+'</td><td>'+(a.serviceDue?'<span class="pill due">Due</span>':'<span class="pill ok">OK</span>')+'</td><td>'+a.openDefects+'</td><td><span class="pill '+(a.status==='In Service'?'ok':'due')+'">'+a.status+'</span></td><td><button type="button" class="mini" data-edit="'+a.id+'">Edit</button></td></tr>').join('');$('rows').querySelectorAll('[data-edit]').forEach(b=>b.addEventListener('click',()=>openEdit(b.dataset.edit)))}function reset(){editingId=null;$('modalTitle').textContent='Add Asset';$('fType').value=assetTypes[0];$('fRego').value='';$('fMake').value='';$('fModel').value='';$('fReading').value='';$('fStatus').value='In Service';$('fLocation').value='';$('fServiceDue').checked=false}function open(){ $('assetModal').classList.add('open')}function close(){ $('assetModal').classList.remove('open')}function openEdit(id){const a=list.find(x=>x.id===id);if(!a)return;editingId=id;$('modalTitle').textContent='Edit Asset';$('fType').value=a.type;$('fRego').value=a.rego;$('fMake').value=a.make;$('fModel').value=a.model;$('fReading').value=a.reading;$('fStatus').value=a.status;$('fLocation').value=a.location||'';$('fServiceDue').checked=!!a.serviceDue;open()}$('addAssetBtn').addEventListener('click',()=>{reset();open()});$('closeModal').addEventListener('click',close);$('cancelModal').addEventListener('click',close);$('search').addEventListener('input',render);$('typeFilter').addEventListener('change',render);$('saveAsset').addEventListener('click',async()=>{const body={type:$('fType').value,rego:$('fRego').value.trim(),make:$('fMake').value.trim(),model:$('fModel').value.trim(),reading:$('fReading').value,status:$('fStatus').value,location:$('fLocation').value.trim(),serviceDue:$('fServiceDue').checked};const url=editingId?'/api/assets/'+editingId:'/api/assets';const method=editingId?'PUT':'POST';const r=await fetch(url,{method,headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(!r.ok)return alert(j.error||'Unable to save asset');close();await load()});load()})();</script>`)));

module.exports=router;
