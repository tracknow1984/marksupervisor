const express=require('express');
const router=express.Router();
const {page}=require('../layout');
const {assets}=require('../store');
const db=require('../persistent-store');
const active=d=>!['RESOLVED','CLOSED'].includes(String(d.status||'').toUpperCase());
function recalc(assetId){const a=assets.find(x=>String(x.id)===String(assetId));if(a)a.openDefects=db.listDefects().filter(d=>String(d.assetId)===String(assetId)&&active(d)).length}
function syncDefectsFromPrestarts(){const defects=db.listDefects(),prestarts=db.listPrestarts();for(const ps of prestarts){for(const fail of (ps.results||[]).filter(r=>String(r.value||'').toLowerCase()==='fail')){if(defects.some(d=>String(d.prestartId)===String(ps.id)&&String(d.prestartItemId)===String(fail.itemId)))continue;const s=String(fail.label||'').toLowerCase(),priority=/brake|steering|tyre|wheel|seat belt|king pin|tow eye|coupling|breakaway|air system|chassis crack/.test(s)?'HIGH':/light|warning|wiper|mirror|leak|suspension|bearing|exhaust/.test(s)?'MEDIUM':'LOW';db.saveDefect({id:'DEF-'+Date.now().toString(36).toUpperCase()+'-'+Math.random().toString(36).slice(2,7).toUpperCase(),assetId:ps.assetId,assetName:ps.assetName,assetType:ps.assetType,rego:ps.rego,prestartId:ps.id,prestartItemId:fail.itemId||null,defect:fail.label||'Pre-Start defect',reportedAt:ps.completedAt||new Date().toISOString(),reportedBy:ps.inspector||'Current User',reading:ps.reading||0,location:ps.address||ps.location||'',priority,status:'OPEN',action:'',resolutionNotes:'',updatedAt:ps.completedAt||new Date().toISOString(),resolvedAt:null,closedAt:null})}recalc(ps.assetId)}}
router.get('/api/vehicle-defects',(req,res)=>{syncDefectsFromPrestarts();res.set('Cache-Control','no-store');res.json(db.listDefects())});
router.patch('/api/vehicle-defects/:id',(req,res)=>{const old=db.listDefects().find(x=>String(x.id)===String(req.params.id));if(!old)return res.status(404).json({error:'Defect not found'});const statuses=['OPEN','IN PROGRESS','RESOLVED','CLOSED'],actions=['','REPAIR','REPLACED','CONTRACTOR'],status=String(req.body?.status||old.status).toUpperCase(),action=String(req.body?.action??old.action??'').toUpperCase(),resolutionNotes=String(req.body?.resolutionNotes??old.resolutionNotes??'').trim();if(!statuses.includes(status))return res.status(400).json({error:'Invalid status'});if(!actions.includes(action))return res.status(400).json({error:'Invalid action'});if(['RESOLVED','CLOSED'].includes(status)&&!action)return res.status(400).json({error:'Select REPAIR, REPLACED or CONTRACTOR before resolving a defect'});const now=new Date().toISOString(),changes={status,action,resolutionNotes,updatedAt:now,resolvedAt:old.resolvedAt,closedAt:old.closedAt};if(status==='RESOLVED'&&!changes.resolvedAt)changes.resolvedAt=now;if(status==='CLOSED'){if(!changes.resolvedAt)changes.resolvedAt=now;changes.closedAt=now}if(!['RESOLVED','CLOSED'].includes(status)){changes.resolvedAt=null;changes.closedAt=null}const d=db.updateDefect(req.params.id,changes);recalc(d.assetId);res.json(d)});
router.get('/vehicle-defects',(req,res)=>{syncDefectsFromPrestarts();res.send(page('defects','Vehicle Defects',`<style>.defectStats{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:18px}.defectStat{background:#fff;border:1px solid #e3e8ef;border-radius:11px;padding:16px}.defectStat span{display:block;font-size:11px;color:#7f8a9a;font-weight:800;text-transform:uppercase}.defectStat b{display:block;font-size:26px;margin-top:5px}.defToolbar{display:flex;gap:10px;justify-content:space-between;margin-bottom:14px}.defToolbar input,.defToolbar select{max-width:280px}.priority{font-weight:800;font-size:11px}.priority.HIGH{color:#b42318}.priority.MEDIUM{color:#ad7410}.priority.LOW{color:#1768c5}.defSelect{min-width:125px}.defNotes{min-width:190px}.archiveHead{margin-top:28px}@media(max-width:800px){.defectStats{grid-template-columns:1fr 1fr}.defToolbar{display:grid}}</style><div class="title"><div><h1>Vehicle Defects</h1><p>Outstanding defects automatically raised from failed Pre-Start items.</p></div></div><div id="stats" class="defectStats"></div><div class="defToolbar"><input id="search" placeholder="Search rego, asset or defect..."><select id="priority"><option value="">All Priorities</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option></select></div><section class="panel"><div class="tablewrap"><table><thead><tr><th>Date</th><th>Priority</th><th>Asset / Rego</th><th>Defect</th><th>Status</th><th>Action</th><th>Resolution Notes</th><th></th></tr></thead><tbody id="openRows"></tbody></table></div></section><div class="sectionhead archiveHead"><div><h2>Defect Archive</h2><div class="sub">Resolved and closed defects remain linked to the asset history.</div></div></div><section class="panel"><div class="tablewrap"><table><thead><tr><th>Raised</th><th>Resolved</th><th>Asset / Rego</th><th>Defect</th><th>Outcome</th><th>Notes</th></tr></thead><tbody id="archiveRows"></tbody></table></div></section><script>(()=>{let rows=[];const $=id=>document.getElementById(id),fmt=v=>v?new Date(v).toLocaleString('en-AU',{dateStyle:'short',timeStyle:'short'}):'—',e=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));async function load(){const r=await fetch('/api/vehicle-defects',{cache:'no-store'});rows=await r.json();render()}function render(){const open=rows.filter(x=>x.status==='OPEN').length,prog=rows.filter(x=>x.status==='IN PROGRESS').length,high=rows.filter(x=>!['RESOLVED','CLOSED'].includes(x.status)&&x.priority==='HIGH').length,arch=rows.filter(x=>['RESOLVED','CLOSED'].includes(x.status)).length;$('stats').innerHTML='<div class="defectStat"><span>Open</span><b>'+open+'</b></div><div class="defectStat"><span>In Progress</span><b>'+prog+'</b></div><div class="defectStat"><span>High Priority</span><b>'+high+'</b></div><div class="defectStat"><span>Archived</span><b>'+arch+'</b></div>';const q=$('search').value.toLowerCase(),p=$('priority').value,live=rows.filter(x=>!['RESOLVED','CLOSED'].includes(x.status)).filter(x=>(!p||x.priority===p)&&(!q||(String(x.rego)+' '+String(x.assetName)+' '+String(x.defect)).toLowerCase().includes(q))).sort((a,b)=>({HIGH:0,MEDIUM:1,LOW:2}[a.priority]-({HIGH:0,MEDIUM:1,LOW:2}[b.priority])||new Date(b.reportedAt)-new Date(a.reportedAt));$('openRows').innerHTML=live.length?live.map(d=>'<tr><td>'+fmt(d.reportedAt)+'</td><td><span class="priority '+d.priority+'">'+d.priority+'</span></td><td><b>'+e(d.rego||d.assetId)+'</b><div class="sub">'+e(d.assetName)+'</div></td><td><b>'+e(d.defect)+'</b><div class="sub">Pre-Start '+e(d.prestartId)+'</div></td><td><select class="defSelect" data-status="'+d.id+'"><option '+(d.status==='OPEN'?'selected':'')+'>OPEN</option><option '+(d.status==='IN PROGRESS'?'selected':'')+'>IN PROGRESS</option><option>RESOLVED</option><option>CLOSED</option></select></td><td><select class="defSelect" data-action="'+d.id+'"><option value="">Select...</option><option '+(d.action==='REPAIR'?'selected':'')+'>REPAIR</option><option '+(d.action==='REPLACED'?'selected':'')+'>REPLACED</option><option '+(d.action==='CONTRACTOR'?'selected':'')+'>CONTRACTOR</option></select></td><td><input class="defNotes" data-notes="'+d.id+'" value="'+e(d.resolutionNotes||'')+'"></td><td><button class="primary" data-save="'+d.id+'">Save</button></td></tr>').join(''):'<tr><td colspan="8"><div class="empty">No outstanding defects.</div></td></tr>';const archived=rows.filter(x=>['RESOLVED','CLOSED'].includes(x.status));$('archiveRows').innerHTML=archived.length?archived.map(d=>'<tr><td>'+fmt(d.reportedAt)+'</td><td>'+fmt(d.resolvedAt||d.closedAt)+'</td><td><b>'+e(d.rego||d.assetId)+'</b><div class="sub">'+e(d.assetName)+'</div></td><td>'+e(d.defect)+'</td><td><span class="pill ok">'+e(d.status)+'</span><div class="sub">'+e(d.action||'—')+'</div></td><td>'+e(d.resolutionNotes||'—')+'</td></tr>').join(''):'<tr><td colspan="6"><div class="empty">No archived defects yet.</div></td></tr>';document.querySelectorAll('[data-save]').forEach(b=>b.onclick=()=>save(b.dataset.save))}async function save(id){const status=document.querySelector('[data-status="'+id+'"]').value,action=document.querySelector('[data-action="'+id+'"]').value,resolutionNotes=document.querySelector('[data-notes="'+id+'"]').value,r=await fetch('/api/vehicle-defects/'+encodeURIComponent(id),{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status,action,resolutionNotes})}),d=await r.json();if(!r.ok)return alert(d.error||'Unable to update defect');load()}$('search').oninput=render;$('priority').onchange=render;load()})();</script>`))});
module.exports=router;
